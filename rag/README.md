# 🧠 MBTI RAG System

Система интеллектуального поиска по документации 16 типов личности MBTI с использованием RAG (Retrieval-Augmented Generation).

## 🎯 Возможности

- **Семантический поиск** — находит информацию по смыслу, а не по ключевым словам
- **42 документа** — полная документация по MBTI, соционике и типам личности
- **16 типов** — детальные описания всех типов
- **Векторная БД** — быстрый поиск с помощью ChromaDB
- **Русский язык** — полная поддержка русскоязычных запросов
- **2 интерфейса** — CLI и веб-приложение

## 📦 Архитектура

```
rag/
├── config.py               # Конфигурация
├── requirements.txt        # Зависимости
├── .env.example           # Пример настроек
├── scripts/
│   ├── indexer.py        # Индексация документов
│   └── query_engine.py   # Поисковый движок
├── app/
│   └── streamlit_app.py  # Веб-интерфейс
├── cli.py                # CLI интерфейс
└── data/
    └── chroma_db/        # Векторная БД (создается после индексации)
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установить зависимости
cd rag
pip install -r requirements.txt
```

### 2. Настройка (опционально)

```bash
# Скопировать пример конфигурации
cp .env.example .env

# Отредактировать .env если нужно
nano .env
```

**Примечание:** OpenAI API ключ нужен только для генерации ответов с помощью GPT. Для поиска по документам он не требуется.

### 3. Индексация документов

```bash
# Запустить индексацию
python scripts/indexer.py
```

Это займет 1-3 минуты. Будут проиндексированы все документы из `docs/` и `types/`.

### 4. Использование

#### CLI (командная строка)

```bash
# Интерактивный режим
python cli.py

# Одиночный запрос
python cli.py "Что такое INTJ?"

# Показать только документы без LLM
python cli.py "когнитивные функции ENFP" --no-llm

# Изменить количество результатов
python cli.py "соционика" -k 10

# Статистика
python cli.py --stats
```

#### Веб-интерфейс (Streamlit)

```bash
# Запустить веб-приложение
streamlit run app/streamlit_app.py
```

Откроется браузер с интерфейсом на `http://localhost:8501`

## 💻 Примеры использования

### Python API

```python
from rag.scripts.query_engine import MBTIQueryEngine

# Инициализация
engine = MBTIQueryEngine(use_llm=False)

# Поиск
results = engine.search("Что такое INTJ?", k=5)

for doc in results:
    print(doc.metadata['filename'])
    print(doc.page_content[:200])
    print()

# Получить статистику
stats = engine.get_collection_stats()
print(f"Индексировано документов: {stats['total_documents']}")
```

### CLI

```bash
# Интерактивный режим
$ python cli.py

🧠 MBTI RAG SYSTEM - Интерактивный режим
💬 Вопрос: Какие когнитивные функции у INTJ?

📚 НАЙДЕНО 5 ДОКУМЕНТОВ
═══════════════════════════════════════════════════════════

[1] INTJ.md
    Раздел: Когнитивные функции
    Фрагмент: INTJ использует следующий стек функций:
    1. Ni (Интровертная Интуиция) — Доминантная...
```

## 📊 Что индексируется

### Документация (docs/):
1. **Основы** (01-09): Введение в MBTI, дихотомии, функции
2. **Углубленная теория** (10-12): Соционика, подтипы А/Т, теневые функции
3. **Отношения** (13-19): Петли, романтика, родительство, дружба, команды, лидерство
4. **Профессиональное** (20-22): Переговоры, конфликты, обучение
5. **Развитие** (23-24): Ментальное здоровье, личностный рост
6. **Социокультурное** (25-29): Гендер, культура, политика, религия, финансы
7. **Практическое** (30-33): Здоровье, хобби, технологии, история
8. **Анализ** (34-38): Персонажи, кейсы, ошибки, мимикрия, корреляции
9. **Специальное** (39-42): Искусство, юмор, животные, путешествия

### Типы (types/):
Детальные описания всех 16 типов:
- **NT**: INTJ, INTP, ENTJ, ENTP
- **NF**: INFJ, INFP, ENFJ, ENFP
- **SJ**: ISTJ, ISFJ, ESTJ, ESFJ
- **SP**: ISTP, ISFP, ESTP, ESFP

## ⚙️ Конфигурация

### Настройки в config.py:

| Параметр | По умолчанию | Описание |
|----------|--------------|----------|
| `EMBEDDING_MODEL` | `paraphrase-multilingual-mpnet-base-v2` | Модель для векторизации |
| `CHUNK_SIZE` | 1000 | Размер фрагмента текста |
| `CHUNK_OVERLAP` | 200 | Перекрытие фрагментов |
| `TOP_K_RESULTS` | 5 | Количество результатов поиска |
| `COLLECTION_NAME` | `mbti_docs` | Имя коллекции в ChromaDB |

### Переменные окружения (.env):

```bash
# OpenAI API Key (опционально, для генерации ответов)
OPENAI_API_KEY=sk-...

# Модель embeddings
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-mpnet-base-v2

# LLM модель (если используется)
LLM_MODEL=gpt-3.5-turbo

# Путь к БД
CHROMA_PERSIST_DIR=./rag/data/chroma_db

# Коллекция
COLLECTION_NAME=mbti_docs

# Параметры поиска
TOP_K_RESULTS=5
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
```

## 🔍 Как работает RAG

```
┌─────────────────────────────────────────┐
│  1. Индексация (выполняется 1 раз)      │
├─────────────────────────────────────────┤
│  Markdown файлы                         │
│       ↓                                 │
│  Chunking (разбиение на фрагменты)      │
│       ↓                                 │
│  Embeddings (векторизация)              │
│       ↓                                 │
│  ChromaDB (векторная БД)                │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  2. Поиск (каждый запрос)               │
├─────────────────────────────────────────┤
│  Вопрос пользователя                    │
│       ↓                                 │
│  Embedding вопроса                      │
│       ↓                                 │
│  Similarity Search в ChromaDB           │
│       ↓                                 │
│  Top-K наиболее релевантных фрагментов  │
│       ↓                                 │
│  [Опционально] LLM для генерации ответа │
│       ↓                                 │
│  Результат пользователю                 │
└─────────────────────────────────────────┘
```

## 📚 Примеры запросов

### Общие вопросы:
- "Что такое MBTI?"
- "Чем отличается INTJ от INTP?"
- "Какие когнитивные функции?"

### Конкретные типы:
- "Сильные стороны ENFP"
- "Как INTJ проявляется в стрессе?"
- "Карьера для INFJ"

### Отношения:
- "Совместимость INTJ и ENFP"
- "Как ENTP дружит?"
- "Конфликты между ESTJ и INFP"

### Теория:
- "Что такое соционика?"
- "Теневые функции"
- "Подтипы А и Т"
- "Квадры в соционике"

## 🛠️ Технологический стек

- **LangChain** — фреймворк для RAG
- **ChromaDB** — векторная база данных
- **Sentence Transformers** — multilingual embeddings
- **Streamlit** — веб-интерфейс
- **Python 3.8+**

## 📈 Производительность

- **Индексация**: ~2-3 минуты для всех документов
- **Поиск**: <1 секунды
- **Память**: ~500MB (с загруженной моделью)
- **Диск**: ~100MB (векторная БД)

## ❓ FAQ

**Q: Нужен ли OpenAI API ключ?**
A: Нет, не обязательно. Система работает и без LLM, возвращая найденные фрагменты документов.

**Q: Можно ли использовать другую embedding модель?**
A: Да, измените `EMBEDDING_MODEL` в config.py. Подойдут любые модели из sentence-transformers.

**Q: Как обновить документацию?**
A: Добавьте/измените файлы в docs/ или types/, затем запустите `python scripts/indexer.py` заново.

**Q: Поддерживается ли английский язык?**
A: Да, используемая модель поддерживает множество языков, включая английский.

**Q: Можно ли развернуть онлайн?**
A: Да, streamlit app можно развернуть на Streamlit Cloud, Heroku, или любом другом хостинге.

## 🔮 Будущие улучшения

- [ ] Hybrid search (keyword + semantic)
- [ ] Reranking для улучшения точности
- [ ] Поддержка PDF документов
- [ ] Multi-query для сложных вопросов
- [ ] Кэширование частых запросов
- [ ] Экспорт результатов в PDF/Markdown
- [ ] Поддержка голосового ввода
- [ ] Мобильное приложение

## 📝 Лицензия

MIT License

## 🤝 Поддержка

При возникновении проблем создайте issue в репозитории.

---

**Создано для эффективного поиска и изучения типологии личности MBTI** 🧠
