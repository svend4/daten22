# Алгоритм Структурирования Данных (Data Structuring Algorithm)
## Уровень 4 → Уровень 3: От Хаоса к Порядку

---

## Введение: Обратное Движение по Пирамиде

Если алгоритм расширения (docs/52) превращает **один вопрос в тысячи подвопросов**, то алгоритм структурирования решает обратную задачу: **превращает миллионы неструктурированных данных в организованные ответы**.

```
УРОВЕНЬ 1: "Города Европы"
    ↓ [АЛГОРИТМ РАСШИРЕНИЯ]
УРОВЕНЬ 2: 850 структурированных вопросов
    ↕ [АЛГОРИТМ СТРУКТУРИРОВАНИЯ] ← МЫ ЗДЕСЬ
УРОВЕНЬ 3: 850 структурированных ответов
    ↑ [ФИЛЬТРАЦИЯ И ПОИСК]
УРОВЕНЬ 4: Миллионы документов (Википедия, статьи, книги)
```

---

## Проблема: Информационная Перегрузка

### Характеристики Уровня 4 (Неструктурированные Данные):

**Объем:**
- Миллионы веб-страниц
- Тысячи книг и статей
- Новости, блоги, форумы
- Видео, изображения
- Научные публикации

**Проблемы:**
- ❌ Отсутствие структуры
- ❌ Дубликаты и противоречия
- ❌ Разный уровень качества
- ❌ Информационный шум
- ❌ Сложность поиска релевантного

**Цель:**
✅ Извлечь только релевантную информацию
✅ Организовать по вопроснику Уровня 2
✅ Устранить дубликаты
✅ Оценить качество
✅ Получить структурированный Уровень 3

---

## Концепция: Вопросник как Магнит

Вопросник Уровня 2 работает как **магнитное поле**, притягивающее релевантные фрагменты из океана данных:

```
УРОВЕНЬ 4 (Хаос):                 УРОВЕНЬ 2 (Вопросник):

┌─────────────────┐              1. Архитектура городов?
│ Paris has many  │ ────→        2. Население Европы?
│ Gothic buildings│              3. Транспортные системы?
└─────────────────┘              4. Культура и традиции?
                                 ...
┌─────────────────┐              850 вопросов
│ The population  │ ────→
│ of London is... │
└─────────────────┘
                                 ↓ СТРУКТУРИРОВАНИЕ
┌─────────────────┐
│ Metro systems   │ ────→        УРОВЕНЬ 3 (Порядок):
│ in Europe...    │
└─────────────────┘              1. [Ответ об архитектуре]
                                 2. [Ответ о населении]
┌─────────────────┐              3. [Ответ о транспорте]
│ Random noise    │ ✗ (отброшено)4. [Ответ о культуре]
└─────────────────┘              ...
```

---

## АЛГОРИТМ: Пошаговый Процесс

### ЭТАП 1: Подготовка Вопросника (Input)

**Вход:** Иерархический вопросник с Уровня 2

**Структура:**

```python
class Question:
    id: str                    # "3.1.2.1" (номер вопроса)
    text: str                  # "Какие готические соборы есть во Франции?"
    archetype: Archetype       # MSCO (Здание)
    keywords: List[str]        # ["готика", "собор", "Франция", "архитектура"]
    priority: int              # 1-5 (важность вопроса)
    expected_answer_type: str  # "list", "number", "text", "boolean"
    context: Dict              # Дополнительная информация
    parent: Question           # Родительский вопрос
    children: List[Question]   # Подвопросы
```

**Действие:**

```python
def prepare_questionnaire(question_tree: QuestionTree) -> SearchIndex:
    """
    Подготавливает вопросник для поиска.
    """
    search_index = SearchIndex()

    for question in question_tree.all_questions():
        # Извлечение ключевых слов
        keywords = extract_keywords(question.text)

        # Добавление контекстных слов из архетипа
        archetype_keywords = ARCHETYPE_KEYWORDS[question.archetype]
        keywords.extend(archetype_keywords)

        # Создание поисковых запросов
        search_queries = generate_search_queries(question, keywords)

        # Индексация
        search_index.add(
            question_id=question.id,
            question=question,
            keywords=keywords,
            queries=search_queries
        )

    return search_index
```

**Пример обработки:**

```
Вопрос: "Какие готические соборы есть во Франции?"

Ключевые слова:
- Прямые: "готический", "собор", "Франция"
- Архетипные (MSCO): "здание", "архитектура", "структура", "сооружение"
- Контекстные: "средневековье", "церковь", "католический"
- Расширенные: "Gothic cathedral France", "Notre-Dame", "Chartres"

Поисковые запросы:
1. "готические соборы Франция"
2. "Gothic cathedrals France"
3. "Notre-Dame Paris architecture"
4. "французская готика список"
5. "medieval cathedrals France"
```

---

### ЭТАП 2: Сбор Данных (Data Collection)

**Источники данных:**

1. **Веб-поиск** (Google, Bing, DuckDuckGo)
2. **Энциклопедии** (Wikipedia, Britannica)
3. **Специализированные базы** (научные статьи, статистика)
4. **Книги** (Google Books, Open Library)
5. **Локальные файлы** (если есть)

**Методы сбора:**

```python
def collect_data(search_index: SearchIndex, sources: List[DataSource]) -> RawDataset:
    """
    Собирает сырые данные из всех источников.
    """
    raw_dataset = RawDataset()

    for question_entry in search_index:
        question_id = question_entry.id
        queries = question_entry.queries

        for source in sources:
            for query in queries:
                # Выполнение поиска
                results = source.search(query, max_results=10)

                for result in results:
                    # Извлечение контента
                    content = extract_content(result)

                    # Сохранение с метаданными
                    raw_dataset.add(
                        question_id=question_id,
                        source=source.name,
                        query=query,
                        url=result.url,
                        title=result.title,
                        content=content,
                        timestamp=now()
                    )

    return raw_dataset
```

**Оптимизация:**

- **Кэширование**: Не искать повторно одинаковые запросы
- **Параллелизация**: Множественные запросы одновременно
- **Ограничения**: Не более N результатов на вопрос (например, 50)

**Пример результата:**

```
Вопрос 3.1.2.1: "Готические соборы Франции"

Собрано документов: 47
Источники:
- Wikipedia: 12 статей
- Britannica: 3 статьи
- Google Books: 8 книг (фрагменты)
- Веб-сайты: 24 страницы

Общий объем: ~250,000 слов
```

---

### ЭТАП 3: Фильтрация и Ранжирование (Filtering & Ranking)

**Цель:** Отобрать только самые релевантные и качественные фрагменты.

**3.1. Релевантность (Relevance Scoring)**

```python
def calculate_relevance(document: Document, question: Question) -> float:
    """
    Вычисляет релевантность документа вопросу.
    Возвращает оценку 0.0 - 1.0
    """
    score = 0.0

    # 1. Совпадение ключевых слов (TF-IDF)
    keyword_score = tfidf_similarity(
        document.content,
        question.keywords
    )
    score += keyword_score * 0.3

    # 2. Семантическое сходство (эмбеддинги)
    semantic_score = embedding_similarity(
        document.content,
        question.text
    )
    score += semantic_score * 0.4

    # 3. Соответствие архетипу
    archetype_score = archetype_match(
        document,
        question.archetype
    )
    score += archetype_score * 0.2

    # 4. Тип ответа
    answer_type_score = answer_type_match(
        document,
        question.expected_answer_type
    )
    score += answer_type_score * 0.1

    return min(score, 1.0)
```

**3.2. Качество (Quality Scoring)**

```python
def calculate_quality(document: Document, source: DataSource) -> float:
    """
    Оценивает качество документа.
    """
    score = 0.0

    # 1. Авторитетность источника
    source_authority = {
        'wikipedia': 0.9,
        'britannica': 0.95,
        'научные_журналы': 1.0,
        'блоги': 0.5,
        'форумы': 0.3
    }
    score += source_authority.get(source.type, 0.5) * 0.3

    # 2. Наличие ссылок и источников
    if document.has_references:
        score += 0.2

    # 3. Актуальность (свежесть)
    age_penalty = calculate_age_penalty(document.date)
    score += (1 - age_penalty) * 0.1

    # 4. Полнота (длина и структура)
    completeness = calculate_completeness(document)
    score += completeness * 0.2

    # 5. Грамматика и читаемость
    readability = calculate_readability(document.content)
    score += readability * 0.2

    return min(score, 1.0)
```

**3.3. Итоговое ранжирование**

```python
def rank_documents(documents: List[Document], question: Question) -> List[ScoredDocument]:
    """
    Ранжирует документы для вопроса.
    """
    scored_docs = []

    for doc in documents:
        relevance = calculate_relevance(doc, question)
        quality = calculate_quality(doc, doc.source)

        # Комбинированная оценка
        final_score = (relevance * 0.7) + (quality * 0.3)

        scored_docs.append(
            ScoredDocument(
                document=doc,
                relevance=relevance,
                quality=quality,
                final_score=final_score
            )
        )

    # Сортировка по итоговой оценке
    scored_docs.sort(key=lambda x: x.final_score, reverse=True)

    return scored_docs
```

---

### ЭТАП 4: Извлечение Фактов (Fact Extraction)

**Цель:** Извлечь конкретные факты из документов.

**4.1. Чанкинг (разбиение на фрагменты)**

```python
def extract_relevant_chunks(document: Document, question: Question) -> List[Chunk]:
    """
    Извлекает релевантные фрагменты из документа.
    """
    # Разбиение на параграфы/предложения
    chunks = split_into_chunks(document.content, chunk_size=500)

    relevant_chunks = []

    for chunk in chunks:
        # Оценка релевантности фрагмента
        chunk_score = calculate_relevance(chunk, question)

        if chunk_score > RELEVANCE_THRESHOLD:
            relevant_chunks.append(
                Chunk(
                    text=chunk.text,
                    score=chunk_score,
                    position=chunk.position,
                    source_doc=document
                )
            )

    return sorted(relevant_chunks, key=lambda x: x.score, reverse=True)
```

**4.2. Извлечение структурированных данных**

Для разных типов ответов используются разные экстракторы:

```python
def extract_facts(chunks: List[Chunk], question: Question) -> Facts:
    """
    Извлекает факты в зависимости от типа ответа.
    """
    answer_type = question.expected_answer_type

    if answer_type == "list":
        return extract_list_facts(chunks, question)
    elif answer_type == "number":
        return extract_numerical_facts(chunks, question)
    elif answer_type == "date":
        return extract_temporal_facts(chunks, question)
    elif answer_type == "text":
        return extract_textual_facts(chunks, question)
    else:
        return extract_generic_facts(chunks, question)


def extract_list_facts(chunks: List[Chunk], question: Question) -> ListFacts:
    """
    Извлекает списки (например, названия соборов).
    """
    all_items = []

    for chunk in chunks:
        # NER (Named Entity Recognition)
        entities = extract_named_entities(chunk.text)

        # Фильтрация по типу (например, только здания)
        relevant_entities = filter_by_archetype(
            entities,
            question.archetype
        )

        for entity in relevant_entities:
            all_items.append(
                ListItem(
                    name=entity.name,
                    type=entity.type,
                    confidence=entity.confidence,
                    source=chunk.source_doc,
                    context=entity.context
                )
            )

    # Дедупликация
    unique_items = deduplicate_items(all_items)

    # Сортировка по уверенности
    unique_items.sort(key=lambda x: x.confidence, reverse=True)

    return ListFacts(items=unique_items)


def extract_numerical_facts(chunks: List[Chunk], question: Question) -> NumericalFacts:
    """
    Извлекает числовые данные.
    """
    numbers = []

    for chunk in chunks:
        # Извлечение чисел с контекстом
        extracted = extract_numbers_with_context(chunk.text)

        for num_data in extracted:
            numbers.append(
                NumericalFact(
                    value=num_data.value,
                    unit=num_data.unit,
                    context=num_data.context,
                    source=chunk.source_doc
                )
            )

    # Валидация (проверка согласованности)
    validated = validate_numerical_facts(numbers)

    return NumericalFacts(values=validated)
```

**Пример извлечения:**

```
Вопрос: "Какие готические соборы есть во Франции?"
Тип ответа: list

Извлеченные факты:
1. Notre-Dame de Paris (уверенность: 0.98)
   - Источник: Wikipedia FR
   - Контекст: "самый известный готический собор Франции"

2. Cathédrale de Chartres (уверенность: 0.95)
   - Источник: Britannica
   - Контекст: "объект ЮНЕСКО, готика XIII века"

3. Cathédrale de Reims (уверенность: 0.94)
   - Источник: Wikipedia FR
   - Контекст: "место коронации французских королей"

4. Cathédrale d'Amiens (уверенность: 0.92)
   - Источник: Google Books
   - Контекст: "крупнейший готический собор Франции"

... [всего 27 соборов]
```

---

### ЭТАП 5: Синтез Ответа (Answer Synthesis)

**Цель:** Объединить извлеченные факты в структурированный ответ.

```python
def synthesize_answer(facts: Facts, question: Question) -> Answer:
    """
    Синтезирует финальный ответ из фактов.
    """
    answer = Answer(question_id=question.id)

    # 1. Агрегация фактов
    aggregated = aggregate_facts(facts)

    # 2. Разрешение противоречий
    resolved = resolve_conflicts(aggregated)

    # 3. Формирование текста ответа
    answer.text = generate_answer_text(resolved, question)

    # 4. Добавление структурированных данных
    answer.structured_data = format_structured_data(resolved)

    # 5. Источники и ссылки
    answer.sources = collect_sources(facts)

    # 6. Метаданные
    answer.confidence = calculate_answer_confidence(facts)
    answer.completeness = calculate_completeness(answer, question)
    answer.last_updated = now()

    return answer
```

**5.1. Разрешение противоречий**

```python
def resolve_conflicts(facts: List[Fact]) -> List[Fact]:
    """
    Разрешает противоречия между фактами из разных источников.
    """
    # Группировка похожих фактов
    fact_groups = group_similar_facts(facts)

    resolved = []

    for group in fact_groups:
        if len(group) == 1:
            # Нет противоречий
            resolved.append(group[0])
        else:
            # Есть несколько версий факта
            # Стратегия: выбор по авторитетности источника
            best_fact = max(group, key=lambda f: f.source_authority)

            # Добавление альтернативных версий в метаданные
            best_fact.alternatives = [f for f in group if f != best_fact]

            resolved.append(best_fact)

    return resolved
```

**5.2. Генерация текста ответа**

Для разных типов вопросов - разные шаблоны:

```python
def generate_answer_text(facts: List[Fact], question: Question) -> str:
    """
    Генерирует читаемый текст ответа.
    """
    if question.expected_answer_type == "list":
        return generate_list_answer(facts, question)
    elif question.expected_answer_type == "number":
        return generate_numerical_answer(facts, question)
    else:
        return generate_textual_answer(facts, question)


def generate_list_answer(facts: ListFacts, question: Question) -> str:
    """
    Генерирует ответ-список.
    """
    items = facts.items[:20]  # Топ-20

    answer = f"Найдено {len(facts.items)} объектов:\n\n"

    for i, item in enumerate(items, 1):
        answer += f"{i}. **{item.name}**"

        if item.context:
            answer += f" - {item.context}"

        answer += "\n"

    if len(facts.items) > 20:
        answer += f"\n... и еще {len(facts.items) - 20} объектов"

    return answer
```

**Пример финального ответа:**

```markdown
## Вопрос 3.1.2.1: Какие готические соборы есть во Франции?

### Ответ:

Найдено 27 готических соборов во Франции:

#### Наиболее значимые:

1. **Notre-Dame de Paris** (Париж)
   - XII-XIV века, высота шпиля 96 м
   - Самый известный готический собор Франции
   - Реконструкция после пожара 2019 года

2. **Cathédrale de Chartres** (Шартр)
   - Объект ЮНЕСКО
   - Знаменитые витражи XII-XIII веков
   - Высота 113 метров

3. **Cathédrale de Reims** (Реймс)
   - Место коронации французских королей
   - XIII век, длина 138 метров
   - 2303 скульптуры

4. **Cathédrale d'Amiens** (Амьен)
   - Крупнейший готический собор Франции
   - Внутренний объем 200,000 м³
   - Объект ЮНЕСКО

5. **Cathédrale de Strasbourg** (Страсбург)
   - Высота 142 м (самый высокий средневековый собор)
   - Астрономические часы
   - Розовый песчаник

... [полный список 27 соборов]

### Статистика:
- Всего соборов: 27
- Объектов ЮНЕСКО: 9
- Средняя высота: 87 метров
- Период строительства: XII-XVI века

### Источники:
1. Wikipedia FR: "Liste des cathédrales gothiques de France"
2. Britannica: "Gothic Architecture in France"
3. UNESCO World Heritage List
4. "French Cathedrals" by Martin Hurlimann (book)

Уверенность: 95%
Последнее обновление: 2026-01-08
```

---

### ЭТАП 6: Валидация и Контроль Качества

**Проверки перед финализацией:**

```python
def validate_answer(answer: Answer, question: Question) -> ValidationResult:
    """
    Валидирует качество ответа.
    """
    checks = []

    # 1. Полнота ответа
    if answer.completeness < 0.5:
        checks.append(Warning("Ответ может быть неполным"))

    # 2. Достаточность источников
    if len(answer.sources) < 2:
        checks.append(Warning("Недостаточно источников для верификации"))

    # 3. Уверенность
    if answer.confidence < 0.7:
        checks.append(Warning("Низкая уверенность в ответе"))

    # 4. Свежесть данных
    if answer.oldest_source_date < (now() - 5.years):
        checks.append(Info("Данные могут быть устаревшими"))

    # 5. Согласованность фактов
    conflicts = detect_conflicts(answer.facts)
    if conflicts:
        checks.append(Error(f"Обнаружены противоречия: {conflicts}"))

    return ValidationResult(
        is_valid=all(c.level != "error" for c in checks),
        checks=checks
    )
```

---

### ЭТАП 7: Построение Уровня 3

**Финальная структура:**

```python
class Level3Structure:
    """
    Структурированная база знаний (Уровень 3).
    """
    topic: str                           # "Города Европы"
    questionnaire: QuestionTree          # Вопросник (Уровень 2)
    answers: Dict[str, Answer]           # question_id -> Answer
    metadata: Metadata

    def get_answer(self, question_id: str) -> Answer:
        """Получить ответ на вопрос."""
        return self.answers.get(question_id)

    def get_section(self, section_id: str) -> Section:
        """Получить раздел (все вопросы архетипа)."""
        pass

    def search(self, query: str) -> List[Answer]:
        """Поиск по ответам."""
        pass

    def export_to_markdown(self) -> str:
        """Экспорт в Markdown."""
        pass

    def export_to_json(self) -> dict:
        """Экспорт в JSON."""
        pass
```

**Пример структуры Уровня 3:**

```
ГОРОДА ЕВРОПЫ - СТРУКТУРИРОВАННАЯ БАЗА ЗНАНИЙ (Уровень 3)
==========================================================

РАЗДЕЛ 1: ФИЗИЧЕСКАЯ СТРУКТУРА
───────────────────────────────

1.1. Базовые Материалы (MSEO)
   [5 вопросов → 5 ответов]

1.3. Архитектура (MSCO)
   [35 вопросов → 35 ответов]

   1.3.1. Исторические стили
      1.3.1.1. Романская архитектура
         ✓ Ответ (уверенность: 92%)
         📊 15 объектов, 8 источников

      1.3.1.2. Готическая архитектура
         ✓ Ответ (уверенность: 95%)
         📊 27 объектов, 12 источников

      ... [всего 35 ответов в разделе]

РАЗДЕЛ 2: ДИНАМИЧЕСКИЕ СИСТЕМЫ
────────────────────────────────

2.6. Население (MDEF)
   [12 вопросов → 12 ответов]

2.7. Транспорт (MDCO)
   [40 вопросов → 40 ответов]

   2.7.1. Метро
      ✓ Ответ (уверенность: 98%)
      📊 61 система метро, 25 источников

   ... [всего 52 ответа в разделе]

РАЗДЕЛ 3: АБСТРАКТНЫЕ ОСНОВЫ
──────────────────────────────

3.9. Статистика (ASEO)
   [18 вопросов → 18 ответов]

   3.9.1. Население городов
      ✓ Ответ (уверенность: 99%)
      📊 Данные по 743 городам

   ... [всего 68 ответов в разделе]

─────────────────────────────────────────────
ИТОГО: 850 вопросов → 847 ответов (99.6%)

Статус:
✓ Полные ответы: 803 (94.5%)
⚠ Частичные ответы: 44 (5.2%)
✗ Нет данных: 3 (0.4%)

Источники данных:
- Wikipedia: 1,247 статей
- Книги: 183 источника
- Научные статьи: 92
- Веб-сайты: 634 страницы

Общий объем: 4.2 млн слов
Уверенность: 91.3%
Последнее обновление: 2026-01-08
```

---

## Оптимизации и Улучшения

### 1. Инкрементальное Обновление

```python
def update_level3(level3: Level3Structure, new_data: RawDataset):
    """
    Обновляет Уровень 3 новыми данными без полного пересчета.
    """
    for question_id, new_docs in new_data.group_by_question():
        existing_answer = level3.get_answer(question_id)

        # Извлечение фактов из новых документов
        new_facts = extract_facts(new_docs, question_id)

        # Мержинг с существующими
        merged_facts = merge_facts(existing_answer.facts, new_facts)

        # Ре-синтез ответа
        updated_answer = synthesize_answer(merged_facts, question_id)

        level3.update_answer(question_id, updated_answer)
```

### 2. Кэширование

```python
# Кэш поисковых запросов
@cache(ttl=7*24*3600)  # 7 дней
def search_web(query: str) -> List[Document]:
    ...

# Кэш эмбеддингов
@cache(ttl=30*24*3600)  # 30 дней
def get_embedding(text: str) -> np.ndarray:
    ...
```

### 3. Параллельная Обработка

```python
def process_questions_parallel(questions: List[Question], max_workers=10):
    """
    Обрабатывает вопросы параллельно.
    """
    with ThreadPoolExecutor(max_workers=max_workers) as executor:
        futures = []

        for question in questions:
            future = executor.submit(process_single_question, question)
            futures.append(future)

        results = [f.result() for f in futures]

    return results
```

---

## Визуализация Процесса

```
УРОВЕНЬ 4 (Хаос)           ФИЛЬТРЫ              УРОВЕНЬ 3 (Порядок)
──────────────────────────────────────────────────────────────────

[1M документов]    →  Релевантность   →   [10K релевантных]
                       (TF-IDF +
                        эмбеддинги)

[10K релевантных]  →  Качество        →   [2K качественных]
                       (источник +
                        свежесть)

[2K качественных]  →  Извлечение      →   [5K фактов]
                       фактов
                       (NER + parsing)

[5K фактов]        →  Дедупликация    →   [3K уникальных]

[3K уникальных]    →  Разрешение      →   [2.5K проверенных]
                       противоречий

[2.5K проверенных] →  Синтез          →   [850 ответов]
                       ответов
                       (агрегация)

══════════════════════════════════════════════════════════════════
Коэффициент сжатия: 1,000,000 документов → 850 ответов
Эффективность: 99.9% шума отфильтровано
```

---

## Метрики Качества

### Оценка Уровня 3:

```python
class Level3Metrics:
    coverage: float          # Процент отвеченных вопросов (95%+)
    avg_confidence: float    # Средняя уверенность (85%+)
    avg_sources: float       # Среднее число источников на ответ (5+)
    freshness: float         # Свежесть данных (80%+ < 1 год)
    consistency: float       # Согласованность фактов (95%+)
    completeness: float      # Полнота ответов (90%+)
```

**Целевые значения для качественного Уровня 3:**

| Метрика | Отлично | Хорошо | Удовлетворительно | Плохо |
|---------|---------|--------|-------------------|-------|
| Coverage | >95% | 85-95% | 70-85% | <70% |
| Confidence | >90% | 80-90% | 70-80% | <70% |
| Sources | >8 | 5-8 | 3-5 | <3 |
| Freshness | >90% | 75-90% | 60-75% | <60% |
| Consistency | >98% | 95-98% | 90-95% | <90% |
| Completeness | >95% | 85-95% | 75-85% | <75% |

---

## Заключение

Алгоритм структурирования данных превращает **информационный хаос в организованное знание**.

### Ключевые достижения:

✅ **Эффективная фильтрация** - отсеивает 99.9% нерелевантной информации
✅ **Структурирование** - организует данные по архетипическому вопроснику
✅ **Валидация** - проверяет качество и разрешает противоречия
✅ **Масштабируемость** - работает с любым объемом данных
✅ **Универсальность** - применим к любой предметной области

### Результат:

**Из миллионов документов → 850 структурированных, проверенных ответов**

**Следующий шаг:** Визуализация всей 4-уровневой пирамиды в блок-схеме.

---

*Документ создан как часть проекта "PseudoRAG: Иерархическая система структурирования знаний"*
